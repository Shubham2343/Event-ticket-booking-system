# Base URL for the backend API.
# Local development: leave unset — the dev server proxies /api to localhost:5000 (see "proxy" in package.json).
# Production (Render): set to your deployed backend URL, e.g.
# REACT_APP_API_URL=https://ticketflow-backend.onrender.com/api
REACT_APP_API_URL=
